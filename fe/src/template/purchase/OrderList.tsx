import React, { useState} from "react";
import styled from "styled-components";

const OrderList = () => {

    return (
        <div>
            <h1>Redux 실습</h1>
        </div>
    );
}

export default OrderList;
